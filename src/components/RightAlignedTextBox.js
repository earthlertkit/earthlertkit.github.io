import React from 'react';
import '../styles/index.css';

const RightAlignedTextBox = ({ children }) => (
  <div className="hero-box right-aligned">
    {children}
  </div>
);

export default RightAlignedTextBox; 